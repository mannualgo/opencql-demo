[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "opencql-inspector"
version = "0.3.0"
description = "Pre-inference context quality inspection for RAG pipelines"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["opencql_inspector*"]
